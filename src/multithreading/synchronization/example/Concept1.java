package multithreading.synchronization.example;

/**
 * Synchronization: Prevents thread interference and memory consistency.
 * Thread Synchronization: Occurs when multiple threads try to read and write shared data concurrently
 *
 */
public class Concept1 {
    public static void main(String[] args) {

    }
}
